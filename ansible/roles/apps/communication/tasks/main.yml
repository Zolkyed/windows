---
- name: Communication | Install packages
  ansible.builtin.include_role:
    name: lib/packages
  vars:
    install_label: Communication
    install_chocolatey_packages: "{{ communication_chocolatey_packages }}"
    install_scoop_packages: []
    install_winget_packages: []
